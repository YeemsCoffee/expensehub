// TODO: Implement ApprovalRules component
// This page will allow admins to manage org-chart-based approval rules
// Format: amount range ’ number of levels up the org chart
// Example: $0-$500 requires 1 level, $500-$5000 requires 2 levels

import React from 'react';

const ApprovalRules = () => {
  return (
    <div>
      <h2>Approval Rules</h2>
      <p>This feature is under development.</p>
      <p>Backend API is ready at: /api/approval-rules</p>
    </div>
  );
};

export default ApprovalRules;
